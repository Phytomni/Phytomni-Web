schema_version = 1

[policy]
default = "deny"

[[exemptions]]
id = "parent-path"
tool = "eslint"
rule = "rule"
classification = "structural"
mechanism = "inline"
target_kind = "symbol"
path = "apps/web/../secret.ts"
target = "name"
